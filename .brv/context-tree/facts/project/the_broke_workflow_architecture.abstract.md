<think>
This is a technical documentation about a project called "the-broke-workflow" (or "broke workflow"). Let me identify the core topic and key insight:

Core topic: A CLI tool with a specific architecture for workflow management

Key insight: The architecture follows a flow from CLI commands → core modules → agents → adapters → steps/templates, with a 6-step playbook

I need to produce a ONE-LINE summary (max 80 tokens) that captures the core topic and key insight. It must be a complete sentence.

Let me draft:

"The Broke Workflow is a CLI tool with a layered architecture flowing from commands to core modules to agents to adapters, using a 6-step playbook and template system."

Let me count tokens