<think>
This is a knowledge document about CLI commands for a task management system. Let me identify the core topic and key insight:

Core topic: Task CLI commands for task lifecycle management
Key insight: Commands cover the full task lifecycle including list, next, show, claim, release, status, deps, dag, add-dependency, comment, comments, and remove

I need to create a ONE-LINE summary (max 80 tokens) that is a complete sentence capturing the core topic and key insight.

Let me draft:
"The bw task CLI provides commands for the complete task lifecycle including list, next, show, claim, release, status, deps, dag, add-dependency, comment, comments, and remove operations."

Let me count tokens